name: Portfolio Keep-Alive

on:
  schedule:
    - cron: '*/5 * * * *' # Every 5 minutes
  workflow_dispatch: # Manual trigger

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Services
        run: |
          echo "--- 🔄 PINGING CLOUD SERVICES ---"
          # IMPORTANT: Replace these with your actual deployed domains!
          curl -s -L https://kushalsaialeti.vercel.app > /dev/null
          # Updated to Render
          curl -s -L https://portfolio-backend.onrender.com/api > /dev/null
          echo "--- ✅ PING SUCCESSFUL ---"
